# One Piece TCG — game server
Room-code matchmaking + accounts + collection sync for the One Piece TCG app.
Deployed on Render via render.yaml (Starter plan, persistent disk for accounts).
Optional env vars for password-recovery emails: RESEND_API_KEY, EMAIL_FROM.
